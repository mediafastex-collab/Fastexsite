# dependencies
node_modules
.pnp
.pnp.js

# next.js
.next/

# NOTE: out/ is intentionally committed.
# Cloudflare Pages has no build command set, so it uploads out/ directly.
# Once the build command is set to `npm run build`, re-add `out/` here.
build

# production
dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# claude code worktrees (nested repos)
.claude/worktrees/

# stray nested repo of the previous static site
Site/
